# Use latest versions and avoid deprecated packages
legacy-peer-deps=false
audit-level=moderate
fund=false
prefer-dedupe=true

# Security settings
audit=true
audit-signatures=true

# Performance settings
progress=false
loglevel=warn
